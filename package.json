{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix drive\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix drive",
    "preview": "npm run preview --prefix drive"
  },
  "dependencies": {
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "concurrently": "^8.2.2",
    "vite": "^8.0.13"
  },
  "engines": {
    "node": "20.x"
  }
}
